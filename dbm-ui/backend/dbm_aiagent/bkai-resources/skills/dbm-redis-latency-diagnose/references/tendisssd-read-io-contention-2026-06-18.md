# TendisSSD 读 IO 争抢全局慢 — 案例记录

**日期**: 2026-06-18 15:51  
**集群**: `ssd23.example.db`（TwemproxyTendisSSDInstance，biz-10 业务）  
**告警**: 集群平均访问耗时 >= 512ms，告警值 591ms，持续约 1 分钟后自恢复

## 诊断数据摘要

### Proxy 层（183 节点）
- 当前 latest 范围：1.3ms ~ 2.7ms（全部正常）
- `cv > 100` 节点数：47 个（统计窗口内曾有尖刺）
- 极端异常节点：`192.0.2.28`  p95=591ms, max=2764ms, latest=2.0ms（已恢复）
- 其余 182 个节点 p95 均 < 3ms

### Master 层（48 节点）
- 延迟 p95 中位数：0.11ms（极低，完全正常）
- 最高 p95：0.138ms（`192.0.2.29`）
- CPU 中位数 p95：6.9%，最高 10.3%

### 慢日志（15:40~16:10，30分钟窗口）
- 总条数：843 条
- 涉及实例：103 个（全集群性）
- 延迟分布：min=10ms, median=247ms, avg=404ms, max=2321ms

**Top Commands**:
| 命令 | 次数 | 占比 |
|------|------|------|
| get | 305 | 36.2% |
| hget | 229 | 27.2% |
| hgetall | 171 | 20.3% |
| hset | 66 | 7.8% |
| hmget | 32 | 3.8% |

**代表性慢 Key 样本**:
```
[get]  3198-3072-9225-5359-94:5:bbcd  1559ms  @ 192.0.2.30:50023
[hget] 9000000007:d:a:b                           943ms   @ 192.0.2.31:50023
[hget] 9000000008:c:a:b                           899ms   @ 192.0.2.32:50023
[get]  9000000009:i:d:c:tool               406ms   @ 192.0.2.33:50023
[get]  9000000010:3:h:g:ps                       218ms   @ 192.0.2.34:50023
```

Key 前缀：`wx:mm`（微信模块）、`sq:mm`（手Q模块）、`glob:escape`/`glob:bhv`（全局行为/事件）、`common_cloud_setting`（云配置）

## 根因判断

- Master 延迟 < 0.14ms 排除 Compaction（Compaction 时 Master CPU 和延迟均会显著升高）
- 慢日志命令以磁盘读（get/hget/hgetall）为主，符合冷数据读取 IO 争抢特征
- 告警触发机制：`192.0.2.28` 等节点历史尖刺被纳入集群聚合 p95 计算，超过 512ms 阈值
- 尖刺持续时间极短（< 1 分钟），自行恢复，属于正常业务高峰期瞬时 IO 压力

## 后续建议

1. 观察是否重复出现（每日 15:50 前后是否固定出现 → 可能是定时任务触发冷读）
2. 业务侧排查 `common_cloud_setting`、`wx:mm:rpi`、`sq:mm:rpi` 等 Key 的访问模式
3. 评估 `hgetall` → `hget` 按字段改造（hgetall 读整个 Hash 是最重的磁盘 IO）
